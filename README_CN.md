# Timestamp CLI

[![Go Version](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)](https://go.dev/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Release](https://img.shields.io/github/v/release/sunshanpeng/timestamp-cli)](https://github.com/sunshanpeng/timestamp-cli/releases)

一个简单快速的时间戳处理命令行工具。

[English Documentation](README.md)

## 功能特性

- ✅ 获取当前时间戳（秒级/毫秒级）
- ✅ 时间戳转人类可读格式
- ✅ 日期字符串转时间戳
- ✅ 智能识别（10位=秒，13位=毫秒）
- ✅ 相对时间解析（如 -5m, +1h）
- ✅ UTC 时区支持
- ✅ 支持多种日期格式

## 安装方式

### 使用 Go Install（推荐）

```bash
go install github.com/sunshanpeng/timestamp-cli/cmd/timestamp@latest
```

### 下载二进制文件

从 [GitHub Releases](https://github.com/sunshanpeng/timestamp-cli/releases) 下载预编译的二进制文件。

### 源码编译

```bash
git clone https://github.com/sunshanpeng/timestamp-cli.git
cd timestamp-cli
go build -o timestamp ./cmd/timestamp
```

## 使用方法

### 获取当前时间信息

```bash
# 显示完整信息（时区、本地时间、时间戳）
timestamp

# 输出示例:
# Timezone: Asia/Shanghai (CST, UTC+8:00)
# Local Time: 2026-02-09 16:48:30
# Timestamp (s): 1738227510
# Timestamp (ms): 1738227510000
```

### 仅获取时间戳

```bash
# 获取秒级时间戳
timestamp -s
# 输出: 1738227510

# 获取毫秒级时间戳
timestamp --ms
# 输出: 1738227510000
```

### 时间戳转日期

```bash
# 10位时间戳（秒级）
timestamp 1768809600
# 输出: 2026-01-19 16:00:00

# 13位时间戳（毫秒级，自动识别）
timestamp 1768809600000
# 输出: 2026-01-19 16:00:00
```

### 日期转时间戳

```bash
# 日期字符串转秒级时间戳
timestamp "2026-01-19 16:00:00"
# 输出: 1768809600

# 日期字符串转毫秒级时间戳
timestamp "2026-01-19 16:00:00" --ms
# 输出: 1768809600000

# 支持多种日期格式
timestamp "2026-01-19"           # YYYY-MM-DD
timestamp "2026/01/19 16:00:00"  # YYYY/MM/DD HH:MM:SS
timestamp "2026/01/19"           # YYYY/MM/DD
```

### 相对时间解析

```bash
# 获取5分钟前的时间戳
timestamp -5m

# 获取1小时后的时间戳
timestamp +1h

# 获取30秒前的毫秒级时间戳
timestamp --ms -30s
```

### UTC 时区支持

```bash
# 显示 UTC 时区信息
timestamp --utc
# 输出: Timezone: UTC (UTC, UTC+0:00)...

# 获取 UTC 时间戳
timestamp -s --utc

# 按 UTC 时区解析时间戳
timestamp 1768809600 --utc

# 按 UTC 时区解析日期字符串
timestamp "2026-01-19 16:00:00" --utc
```

### 显示时区信息

```bash
timestamp --tz
# 输出: Asia/Shanghai (CST, UTC+8:00)
```

### 版本信息

```bash
timestamp -v
# 输出: timestamp version 1.0.0
```

## 命令行参数

| 参数 | 简写 | 说明 |
|------|------|------|
| `--help` | `-h` | 显示帮助信息 |
| `--version` | `-v` | 显示版本信息 |
| `--second` | `-s` | 仅输出秒级时间戳 |
| `--ms` | | 输出/使用毫秒级时间戳 |
| `--utc` | | 使用 UTC 时区（默认本地时区） |
| `--tz` | | 显示时区信息 |

## 支持的日期格式

- `2006-01-02 15:04:05`（标准日期时间）
- `2006-01-02`（仅日期）
- `2006/01/02 15:04:05`（斜线分隔的日期时间）
- `2006/01/02`（斜线分隔的日期）

## 支持的相对时间单位

- `s`: 秒 (例如 `-30s`)
- `m`: 分 (例如 `-5m`)
- `h`: 时 (例如 `+1h`)
- `d`: 天 (例如 `-1d`)

## 使用场景

- 🔧 开发调试：快速查询时间戳
- 📝 脚本编写：在 Shell 脚本中获取时间戳
- 📊 日志分析：转换日志中的时间戳
- ⏰ 时间计算：在不同时间格式之间转换

## 使用示例

```bash
# 在脚本中获取当前时间戳
NOW=$(timestamp -s)
echo "当前时间戳: $NOW"

# 转换日志中的时间戳为可读格式
timestamp 1768809600
# 输出: 2026-01-19 16:00:00

# 获取 UTC 时间戳用于 API 调用
timestamp -s --utc
```

## 开发

### 运行测试

```bash
go test -v ./...
```

### 运行测试（带覆盖率）

```bash
go test -cover ./...
```

### 构建

```bash
go build -o timestamp ./cmd/timestamp
```

## 贡献

欢迎贡献！请随时提交 Pull Request。

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件。

## 作者

**sunshanpeng** - [sunshanpeng@outlook.com](mailto:sunshanpeng@outlook.com)

## 仓库地址

https://github.com/sunshanpeng/timestamp-cli
